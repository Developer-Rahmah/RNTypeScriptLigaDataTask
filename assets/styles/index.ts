const {default: Colors} = require('./Colors')
const {default: General} = require('./General')
const {default: Fonts} = require('./Fonts')
const {default: Elements} = require('./Elements')
const {default: Layout} = require('./Layout')
const {default: ImageStyles} = require('./ImageStyles')

export default {
  Colors,
  Fonts,
  General,
  Elements,
  Layout,
  ImageStyles,
}
